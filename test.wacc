begin
  int f(int a, int b) is
    a = 5;
    a = 6;
    a = 7;
    a = 8;
    b = a + a + a;
    b = a + a;
    return a * b * c
  end

  int a = 5;
  int b = 6;
  while b < 10 && (a > 10 || a < 20) && b < 20 do
    print f(a, b);
    b = b + 1
  done
end